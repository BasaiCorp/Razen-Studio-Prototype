# New Razen Syntax Test File

struct userData {
    name<str>,
    age<num>
}

var any_type_value = "Prathmesh" # any type data

var<num> x = 10
var<num> y = 4.26

var<str> name = "Razen"

var<bool> isAdmin = false

var<map> Foods = {
    "Chai": 40,
    "Coffee": 40,
    "Dosa": 60
}

var<list> Fruits = ["Apple", "Banana", "Cheery"]

var<arr> Numbers = [1, 2, 3, 4]
var<arr<num>> Numbers_same = Numbers

# This part is commented out as 'User' is not defined as a callable constructor
# var<obj> User = User("Prathmesh", 16)

var<tuple> Words = ("Apple", "Banana", "Dog")

read choise
read<num> number
read<str> name_input
read<bool> condition

if (number<num>) {
    show<green> "This is a number"
} else {
    show<red> "This is Not a number hehe"
}

while (true) {
    # F-string test
    show f"Hello, {name}! Your number is {number}."
}

match x {
    10: show "x is 10",
    default: show "x is not 10"
}

# Standalone types should not be highlighted
num str bool map list arr obj tuple
